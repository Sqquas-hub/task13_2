# task13_2
Напишите функцию find_keywords(), которая принимает на вход путь к файлу и выполняет следующие действия:

Загружает данные из указанного CSV-файла в DataFrame.
Для каждого предобработанного отзыва (столбец «normalized_text») извлекает ключевые слова и оценку уверенности модели для каждого из них. Результаты сохраняются в виде списка кортежей в новый столбец «key_words». Для извлечения используйте один из предложенных в лекции вариантов.
Возвращает обновленный DataFrame.
